# Go library fixtures

These inputs are intentionally portable. `@ROOT@` is replaced with a temporary
directory by the Go loader tests, and the manifest fixes video modification times
because Git does not preserve them.
